# Pack-facing contracts

The `dkg.pack.v1` schema and event/ontology contracts are versioned in
[`fossil-core`](https://github.com/Pukujan/fossil-core). This repository keeps
the manifest and portable pack paths needed to identify and mount this pack;
it does not copy fossil-core implementation code.
